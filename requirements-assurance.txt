# The shared assurance lane (FR-006). Install this in its OWN environment.
#
# It is deliberately separate from the system interpreter. Nothing in this
# repository imports jsonschema: the Draft 7 lane belonged to the local evidence
# schemas and the collector, both of which are gone, so there is no version
# conflict left to resolve. The environment exists for a different reason:
# engineering-assurance is pinned as a
# git tag, and resolving a git dependency into the system interpreter would make
# the pin depend on whatever else that interpreter happens to have.
#
# This candidate pin becomes active only after the v0.3.2 matrix has named
# human acceptance and the exact source-distribution tag exists. A branch head
# does not substitute for the released artifact.
engineering-assurance @ git+https://github.com/agent-ix/engineering-assurance@v0.3.2
