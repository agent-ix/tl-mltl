---
id: AA-001
title: tl-mltl v0.1 source-release argument
type: AssuranceArgument
status: active
owner: tl-mltl-release-owner
profile: ix://agent-ix/tl-mltl/AP-001
top_claim:
  id: claim-v0.1-source-release
  statement: the exact tl-mltl v0.1 candidate is acceptable as a bounded reference implementation
  subject: one source revision, syntax revision, corpus revision, and evidence manifest
  status: open
reasoning:
  - id: reasoning-conformance
    statement: complete semantic, horizon, prefix, mapping, and evidence obligations reduce uncertainty for the bounded intended use
    supports: claim-v0.1-source-release
    sufficiency_criteria:
      - every applicable matrix row has current evidence
      - reviews have no unresolved blocking finding
      - the human release owner accepts the named revisions and limitations
assumptions:
  - id: assumption-syntax-valid
    statement: inputs admitted by the exact tl-syntax revision satisfy its structural contract
    owner: tl-mltl-release-owner
    status: confirmed
    review_by: "2027-08-30T00:00:00Z"
participants:
  - id: tl-mltl-release-owner
    role: human source-release decision owner
    authority: accept or reject the exact v0.1 candidate
    independence: reviews agent-produced implementation and evidence
challenges:
  - id: challenge-upstream-syntax-merge
    target: claim-v0.1-source-release
    statement: the compiled tl-syntax pin moved from 740182f13b84858008d6f176f75136737d405c1b, reachable only from a pull-request branch, to 953ee825e5060335b4c79682f5f41a78c5a1bfae on tl-syntax main; the retained shared-corpus bytes remain the 740182f1 copy and the two revisions are separate declared facts
    status: resolved
    owner: tl-mltl-release-owner
  - id: challenge-make-execution-controls
    target: claim-v0.1-source-release
    statement: removing the Makefile parse-time guard block and check_failure_propagation.py leaves Make's own execution controls unpoliced; measured here, a single .IGNORE line turns 10 failing ci prerequisites green and takes make ci from exit 2 to exit 0, and the structural replacement covers only the seven producers that feed the chain
    status: open
    owner: tl-mltl-release-owner
  - id: challenge-external-monitor
    target: claim-v0.1-source-release
    statement: external R2U2 availability and project-specific monitor qualification remain outside the source-release claim
    status: open
    owner: temporal-program-owner
relationships:
  - target: ix://agent-ix/tl-mltl/AP-001
    type: references
---

# tl-mltl v0.1 source-release argument

## Claim

The open claim concerns only the exact source, syntax dependency, corpus, and
evidence revisions presented to the human owner. It excludes downstream monitor
qualification and crates.io publication.

## Reasoning

Complete semantic, resource, prefix, mapping, and differential obligations
reduce uncertainty for the bounded intended use. Human review owns sufficiency.

## Sufficiency Decision

The claim remains open until the retained evidence names the exact candidate,
all blocking gaps close, and the human owner records an explicit decision.
Automated checks cannot change this status.

## Challenges

The `tl-syntax` supply-chain exception is discharged. The compiled pin is now
`953ee825e5060335b4c79682f5f41a78c5a1bfae`, which is reachable from tl-syntax
`main`, so the exception that permitted a pull-request revision no longer
applies and its 2026-09-07 expiry is moot. The corpus basis deliberately did not
move: `corpus/tl-syntax-v1` remains the byte-identical copy taken at
`740182f13b84858008d6f176f75136737d405c1b` and verified by its own `SHA256SUMS`.
Both revisions are declared in `assurance/pins.json` and cross-checked on every
run, and collapsing them into one string is refused.

External R2U2 availability and project-specific monitor qualification remain
outside this source-release claim and cannot be closed by local emulation. The
retained R2U2 4.2 exchange is evidence of agreement at one external revision. No
gate re-executes R2U2 or C2PO, deliberately, so a change in external behaviour
after `4.2-release` would not be observed here.

**The Make execution-control guard is gone and this argument does not pretend
otherwise.** The parse-time guard block in the `Makefile` and
`scripts/check_failure_propagation.py` were removed with the collector they were
protecting. The class they guarded is real and was measured in this repository:
with a syntax error introduced into `src/lib.rs`, `make -k ci` exits 2 and 10
of the 14 `ci` prerequisites do not complete while 4 still do; adding a single
`.IGNORE:` line makes all 10 report success and `make ci` exits 0. What replaces the guard is
structural — Quoin binds each retained input by digest and every attested result
is derived from the producer's own bytes, so a producer that did not run yields
an absent or empty input the chain refuses — and it covers only the seven proofs
re-run inside `make assurance-inputs`. `fmt-check`, `lint`, `check-corpus`,
`deny`, `audit-unsafe`, `rustdoc`, and the `quire validate` half of `spec` feed
no input to the chain and are simply neutered; `.IGNORE:` also suppresses the
chain's own refusal. This is recorded as an open challenge above, as an open
unknown in `assurance/change-assurance.json`, and as `agent-ix/tl-mltl#14`. It is
not claimed to be closed.

The host-scoped executable census in `tools.lock`, and the claims this argument
previously made about it, are withdrawn along with the file. A second party can
now reproduce every gate: nothing in the gate set depends on one operator's
absolute paths or home directory. That is a strengthening of reproducibility and
a weakening of executable-identity binding at the same time, and both halves are
stated.

## Retained Archive

All six retained records, including
`tl-mltl-v01-202f8de64ea8-20260831T060946Z` at candidate revision
`202f8de64ea8dd06d4c8bb9bf7972644817fabc7`, remain byte-for-byte unchanged: 283
files under `evidence/`, read on every run through the pinned Engineering
Assurance mapping and never written. `evidence/ANCHORS` and
`evidence/RETRACTIONS.json` survive as retained bytes; nothing interprets them
any more, because the verifier that did was removed with the rest of the local
framework. The dispositions those files record still stand as history and none
of the six records supports this claim.

Read through `map_pgm01_bytes` at the pinned release, all six envelopes classify
`incompatible` with the reason `unknown PGM-01 schema version`: their family is
`quire.derivation-evidence/v1`, which the PGM-01 mapping does not cover. That
answer was measured here, not assumed from a sibling — the same question returns
a mapped result in `quire-contract-ir` and `unreadable` in `quire-analyze`. It is
reported as it stands and is filed upstream as
`agent-ix/engineering-assurance#21`.

The claim remains open. It is not closed by this migration, and no local digest,
sealed record, or verification receipt in this repository confers a release
decision. The receipt this change produces reads `incomplete` with a missing
human decision, which is the correct answer while no ix-flow decision event
exists.
