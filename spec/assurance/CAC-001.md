---
id: CAC-001
title: tl-mltl semantic oracle contract
type: ComponentAssuranceContract
status: active
owner: tl-mltl-maintainer
kind: deterministic
responsibility: produce traceable context-bound finite-trace verdicts and checked resource bounds
inputs: [validated formula, semantic profile, ordered proposition trace, evaluation limits, optional external tool identity, optional shared signal catalog and requirement context]
outputs: [Boolean or pending verdict, horizon report, mapping manifest, differential record, explicit error]
invariants: [no unknown future becomes Boolean success, arithmetic never wraps, every record preserves identities, contextual agreement requires matching shared context]
failure_behaviors: [reject unsupported profile, reject malformed trace, reject resource excess, reject unresolved or unsupported signal mapping, retain external tool failure and context mismatch]
version_pins:
  rust-msrv: "1.75"
  tl-syntax-corpus: tl-syntax-corpus/v1
  evaluation-schema: tl-mltl.evaluation/v1
  contextual-evaluation-schema: tl-mltl.evaluation/v2
  contextual-horizon-schema: tl-mltl.horizon/v2
  mapping-schema: tl-mltl.monitor-mapping/v1
  contextual-mapping-schema: tl-mltl.monitor-mapping/v2
  contextual-external-schema: tl-mltl.external-verdict/v2
  contextual-differential-schema: tl-mltl.differential/v2
controls:
  surfaces: [validated tl-syntax Formula, shared tl-syntax signal and requirement-context documents, checked horizon function, typed evaluation APIs, versioned CLI documents]
  fallback: return pending or an explicit error
  abstention: emit no Boolean or executable adapter artifact on unsupported input
  escalation: require human review for semantic profile or external adapter changes
isolation: parser, rewrite engine, production monitor, and consuming project remain external
replacement: preserve semantic fixtures, error taxonomy, schema identities, resource checks, and evidence digests
relationships:
  - target: ix://agent-ix/tl-mltl/AP-001
    type: references
---

# tl-mltl semantic oracle contract

## Component Boundary

The component begins with an already validated tl-syntax Formula plus an
ordered trace. Contextual operations also receive the shared catalog and
optional shared requirement context. It ends with a typed native report or
explicit error. Parsing, rewriting, contract-IR translation, generic evidence
retention, and external execution remain outside.

## Required Behavior

The core returns only values justified by the named profile and complete input.
Closed evaluation, prefix evaluation, and horizon analysis agree at decision
boundaries. Unsupported mapping or external-tool failure cannot become a pass.
Contextual reports bind the complete catalog and context, and differential
agreement requires the external result to identify the same values.

## Failure Handling

Profile mismatch, malformed traces, overflow, work-budget exhaustion, and
unresolved bindings, unsupported signal names, context mismatch, and unsupported
mapping return distinct errors or non-success states. No fallback Boolean or
executable mapping is emitted.

## Controls

Checked horizon arithmetic, configured work/span budgets, and a fixed
recursion-depth boundary gate temporal expansion. Versioned schemas, source
pins, and content digests gate exchange.

## Replacement

A replacement must pass the same shared and differential corpus, retain exact
identity fields, and demonstrate that arithmetic and work limits fail closed.
